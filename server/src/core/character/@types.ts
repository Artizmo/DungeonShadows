export interface Position {
  x: number;
  y: number;
}
export interface Stats {
  hp: number;
  maxHp: number;
}

export interface Zone {
  id: string;
  areaId: string;
  mapPath: string;
}
