export interface Position {
  x: number;
  y: number;
}
export interface Stats {
  hp: number;
  maxHp: number;
  mana: number;
  maxMana: number;
  speed: number;
}

export interface Zone {
  id: string;
  areaId: string;
  mapName: string;
}
